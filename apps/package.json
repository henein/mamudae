{
  "name": "@henein/henein",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nx next:dev",
    "build": "nx next:build",
    "start": "nx next:start",
    "lint": "next lint",
    "test": "nx jest:test"
  }
}
